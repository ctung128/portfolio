@AGENTS.md

## Media

Run `node scripts/optimize-media.mjs --write` after adding images/videos: it converts referenced PNG/JPGs to WebP (lossless for UI screenshots, q82–q90 for gradients/photos/covers, max 2000px wide), re-encodes MP4s (CRF 22, no audio), rewrites the paths in `src/`, and moves originals to the git-ignored `media-originals/`. Use `<LazyVideo>` instead of `<video autoPlay>`, and `loading="lazy"` on below-the-fold `<img>`s. Keep `/brand/social-preview.jpg` a JPEG (link unfurlers often skip WebP).

## Project log

Running log of notable work done with Claude Code, newest first. Keep entries to a few bullets — this is a changelog, not a task tracker.

### 2026-09-24
- ↗ arrows use `ARROW_NE` (`src/lib/glyphs.ts`, "↗" + U+FE0E) so iOS shows a plain symbol, not a color emoji; use it for any new ↗. About heading → bio gap is 12px below lg (was 24px everywhere).
- Case studies: removed the mobile/tablet "On this page" dropdown (`MobileTableOfContents` deleted); the desktop sidebar TOC is unchanged. About pillar cards smaller on all devices: titles 20/24px (were 24/30), list text 15px (was 16), padding p-6/p-8 (was p-8/p-10).
- Mobile/tablet type scale (desktop `lg:` sizes unchanged): page titles 28 → 34–38 → 36–48px; homepage/About h2 26 → 30 → 36; section + subsection headings 22 → 26 → 30; stats 28 → 32 → 36; quotes/callouts/card and row titles 20 → 22 → 24; wrapped hero headline 28px.
- Responsive audit: all 7 pages × 8 widths (320–1440) have no horizontal overflow and no text under 11px on phones. Fixes: hero headline wraps at 30px under a 560px container instead of shrink-to-fit (it was dropping to ~17px on phones, smaller than body text); header drops the hidden CTA's grid column on mobile and tightens nav under 360px so the logo stays 86px (was squeezed to 49px at 320px); Techstars pill loads eagerly (no tag reflow).
- Deploy check: `next build` clean (all 9 routes static), tsc + eslint clean, all 132 referenced assets tracked in git with exact-case paths (Vercel is case-sensitive), no env vars, no console errors in a production run. Fixed doubled page titles ("— Carolyn Tung — Carolyn Tung"): case study metadata now returns just the title and the root template appends the name. New covers: Jino `cover.png` (3.1 MB → 196 KB), MySupplementals `cover.png` (fixes its empty homepage card); Jino/Props phone videos get first-frame `cover-poster.webp` (`heroMockup.poster` / `duoHero.poster`) so they never show black.
- Homepage order for recruiters: SplitEV → EVA → Props → MySupplementals → Jino (shipped work with hard numbers first, in-demand AI second, Jino last until beta results land). MySupplementals year is now "Shipped 2026".
- Play: fixed the Grean card's broken image (stray `]` in `/play/project-eight/cover.png]`), optimized it (506 KB → 71 KB WebP), moved Grean to row 1 first and Queue management to row 2 middle.
- SplitEV: merged Onboarding, Booking, Driver dashboard, and Vehicle management into one `solution` section (TOC: "Solution"); each former section's heading (plus Host dashboard's) is now a serif `heading` subheading.
- SplitEV: "Before the redesign" eyebrow + iPhone 13 `mockup` of `before-1..4` on `#EFF9EF` under the Friction everywhere quotes (mockup backgrounds now accept a hex color; `before-4` recomposed to phone size by trimming empty space above the car, original in `media-originals/`). Host dashboard placeholder replaced with `host-1`/`host-2` stacked vertically.
- Image borders are now translucent (`border-black/10`) so they stay visible on tinted screenshots (the fixed `#e0e6e0` vanished on SplitEV's sage images); unchanged-looking on white ones.
- Section `heading` is optional (renders just the eyebrow); Props Final design drops its "Final design outputs" h2.
- SplitEV onboarding: replaced the missing `onboarding-before-after.png` with `driver-journey` and `host-journey` maps stacked (lossless WebP at 2000px, 1.26 MB → 158 KB); the "24 screens down to 10" caption sits under the host map.
- Props: new Testing section (between Final design and Iteration): RITE setup in one line, three before → after `stats` (0 → 3/3 understood the value prop, 10 → ≤2 taps to an Eventure, 25% → 100% task completion), and an arrow list tying each to a retention risk, with a one-line caveat that these are test results, not production analytics.
- Props final design: each of the six outputs is now a serif subheading + a new `mockupRow` block (Before / After iPhone 13 mockups side by side on `cover-bg.webp`, black eyebrow labels above the cards for AA). Complete Quest and Eventure Profile afters are static (`after-4`, `after-3`); `after-33.webp` is no longer used. "Before" cards and the "Before the redesign" mockup use Jino's off-white `bg-fcfffa.png`; the "after" cards now do too (`AFTER_BG` in `props.ts`, trialing a calmer look — set it back to `cover-bg.webp` for the green); a → arrow sits between cards. After-screens converted to lossless WebP. Post-launch scroll sped up 1.5× (`holdMs: 930`, `scrollMs: 1730`).
- `ScrollSequence`/`ScrollingImage`: screenshots shorter than the screen now fill it (`min-h-full object-cover`) instead of leaving a black strip; pinned bars only render when the frame actually scrolls.
- Props: post-launch iteration gets an iPhone 13 `mockup` on `cover-bg.webp` using new `scrollFrames`: each full-length screenshot (`final-1..3.webp`, 393×1103) holds at the top, scrolls to its bottom, holds, then crossfades to the next, looping; `pinTop: 54` / `pinBottom: 83` keep the status bar and tab bar fixed while content scrolls.
- Mockup scroll distance is now measured at runtime (`useScrollDistance` in `IPhoneMockup`) so pans stop exactly at the image's bottom edge. Removed `scrollImage.endPercent`, whose fixed 9:19.5 math overshot (the screen inside the bezel is taller), leaving ~19px of black under Jino's empty state.
- New `details` block (label/value grid styled like the header's Role / Timeline / Team; array values stack like Team). Props overview uses it for Business goals / Timeline / Technical constraints.
- Props onboarding: new `featureRows` block (phone-mockup video left, serif title + description right, stacks on mobile; modeled on rachelchen.tech/projects/pokergpt's solution rows) showing the cut-scene, quest, and tutorial concepts. Videos optimized 19.3 MB → 2.7 MB, with q88 WebP first-frame posters (`*-poster.webp`) so phones never show black. Mockups take a `poster`. Replaced the missing `onboarding-explorations.png` placeholder and the bullet list.
- Load performance: referenced media 118 MB → 27 MB (`scripts/optimize-media.mjs`; biggest wins were the `cover-bg.png` gradients, 4–8 MB → ~100 KB each, and Props `cover.mp4`, 28 MB → 3.4 MB). Videos now use `LazyVideo` (download/play only near the viewport, pause off-screen); `CaseStudyImage` uses `useImgFailed` instead of JS-preloading every image, so `loading="lazy"` works. Homepage initial media ~60 MB → ~1 MB. Unused `.mov` sources and the duplicate `props/props.mp4` moved to `media-originals/unused/`.
- Props problem section: animated iPhone 13 Pro `mockup` cycling the six original-app screens (`before-1..6.png`) on `cover-bg.png`, below the stats, under a "Before the redesign" eyebrow subheading (outside the card, ink on the page background for contrast).
- EVA screenshots sit on a `#E6EFF2` tint card (from EVA's own pale blue-gray surfaces): full screens in a browser frame, cropped components floating. Two-column `comparison` cards (citations, Before/After) keep the default off-white so the floating screens stand out. `crossfade` now takes `background` (renders in a `BrowserFrame`, extracted from `CaseStudyImage`).
- EVA problem section: insights now use the Jino-style `insights` block (label + arrow, `quote` is now optional); problem statement and HMW use a new `callout` block (eyebrow label above non-italic serif text) instead of an attributed italic quote.
- EVA iterations: iterations are serif headings with a `kicker` ("Iteration 1"); rejected citation explorations sit side-by-side in a `comparison` with outlined "Rejected" chips; shipped features get a filled "Shipped" chip (`status` on subheadings and comparison groups). The section's own eyebrow + heading are hidden via a new `hideHeader` section option, so "Iteration 1" opens it.
- New MySupplementals case study (`/work/mysupplementals`): overview, problem/stakeholders, opportunity, four design iterations, outcome with founder testimonial, and a closing reflection on opinionated design.
- Case study header: optional `liveUrl` renders a "See it live ↗" button next to the one-liner.
- New `video` block type: autoplaying looping video styled like an image card.
- Favicon: light/dark variants (`public/brand/favicon-light.svg` / `favicon-dark.svg`) wired via `metadata.icons` with `prefers-color-scheme` media queries; removed `src/app/icon.svg`.
- Screenshot framing: image blocks take `frame: "browser" | "float"` on a `background` card; comparison blocks take `float`; paragraphs take an inline `link`. MySupplementals uses a `#F1F7FA` tint.

### 2026-09-21
- About section: redesigned the Reading/Listening/Learning/Recommending pillar cards to match the original Framer site — moved out of the cramped half-column into a full-width 2x2 grid below the bio, `#F7F7F7` background with a custom drop shadow, larger type/padding/star bullets.
- About content: new bio intro ("Hi there! I'm a product designer..."), removed "Stay True", "Farewell My Concubine", "Blockchain Chicken Farm"; swapped "Philosophy of aesthetics" for "A freelance portrait photography studio".
- CTA buttons: hover now triggers on the whole pill (was arrow-glyph-only via `:has()`); the arrow slides in sync with the label instead of sitting static.
- Design tokens: renamed `--color-blue` to `--color-green` (`#628A00`), consolidated across all usages.
- Case study cards: removed numbering and description text, renamed "Props MVP Redesign" to "Props", added "Shipped <year>" labels to SplitEV/Jino/Inkline.
- Installed GitHub CLI (`gh`) locally. (Superseded: the repo lives at `github.com:ctung128/portfolio` and `main` is pushed.)
